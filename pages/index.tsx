import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Quik | A blazing fast web IDE</title>
      </Head>
    </div>
  );
};

export default Home;
